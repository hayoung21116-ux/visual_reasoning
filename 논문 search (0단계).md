## 회전에 강건한 Visual Reasoning — 아이디어 소스 논문 서베이 (0단계 요약)

> 각 논문을 **0단계**(기존 방법 문제점 한 줄 + 핵심 아이디어 한 줄 + 중학생 수준 한 문단)로만 정리했습니다.
> 더 궁금한 논문이 생기면 그것만 1단계(핵심 실험 세팅·결과)로 내려가면 됩니다.
> 요약은 초록·서베이 기반이라 수치나 세부 세팅은 원문 확인이 필요합니다.

**전제(진단은 이미 끝난 걸로 두고 갑니다):** RotBench(arXiv 2508.13968)에 따르면 <cite index="5-1">GPT-5, o3, Gemini-2.5-Pro조차 입력 이미지의 회전을 안정적으로 맞히지 못하고, 특히 90°와 270°를 구분하는 모델은 하나도 없습니다</cite>. 아래 논거에서 이 사실만 가끔 인용합니다.

---

## 어떻게 나눴는지 (지도)

| 그룹 | 접근 | 사용자 아이디어와의 관계 |
|---|---|---|
| **A** | **앞단에 작은 모델을 붙여 정방향으로 되돌리기** (canonicalization) | ★ 사용자 아이디어와 사실상 동일 계열 |
| **B** | **본 모델이 스스로 툴콜해서 회전시키기** (thinking with images) | ★ 사용자 아이디어의 다른 버전 |
| **C** | 툴 없이 그 능력을 모델 안에 distill해 넣기 | 툴콜의 지연·비용 문제에 대한 반론 |

---

# A. 앞단에 작은 모델을 붙여 정방향으로 되돌리기 ★

> "소형 모델을 앞단에 붙여 정방향으로 회전시킨 뒤 본 모델에 태운다"는 아이디어는 학계에서 **canonicalization**이라는 이름으로 이미 이론 틀이 잡혀 있습니다. 인용 근거로 쓰기에 가장 좋은 계열입니다.

## A-1. Equivariance with Learned Canonicalization Functions (arXiv 2211.06489, Kaba et al.)

- **기존 문제점:** 회전에 강건하게 만들려면 보통 네트워크 구조 자체를 equivariant하게 다시 설계해야 하는데, 이게 어렵고 느리다.
- **핵심 아이디어:** 구조는 그대로 두고, **입력을 표준 자세로 되돌리는 작은 네트워크를 앞에 하나 붙인다.**

물건을 알아보는 방법에는 크게 세 가지가 있습니다. ① 방향과 무관한 특징만 보기, ② 모든 방향의 모습을 다 외워두기, ③ **일단 표준 방향으로 돌려놓고 나서 알아보기.** 이 논문은 ③을 딥러닝으로 구현합니다. <cite index="33-1">핵심 가설은 canonicalization을 미리 정해둔 휴리스틱으로 하기보다 작은 신경망으로 학습시키는 게 낫다는 것이고, 실험 결과 이 방식이 기존 equivariant 학습 기법들과 경쟁력 있는 성능을 보였습니다</cite>. <cite index="35-1">이 canonicalization function은 equivariant하지 않은 백본 아키텍처에 그대로 꽂아 쓸 수 있습니다.</cite>

**→ 아이디어 씨앗:** 여기서 말하는 '작은 네트워크'를 **소형 reasoning 모델 + 툴콜**로 바꾸는 게 곧 과제의 novelty 후보입니다. 기존 canonicalizer는 회전 각도를 회귀/분류로 뱉는 미분 가능한 모듈인데, 사용자 구상은 "추론해서 툴을 부르는" 이산적 에이전트입니다. **이 대체가 정당한 이유**(회전 판단에 semantic 이해가 필요하다는 점)를 논증하면 그 자체로 기여가 됩니다.

## A-2. Equivariant Adaptation of Large Pretrained Models / EquiAdapt (NeurIPS 2023, arXiv 2310.01647, Mondal et al.)

- **기존 문제점:** canonicalization network를 그냥 붙이면, 그게 만들어낸 "표준 자세"가 **본 모델이 학습 때 봤던 자세와 어긋나서** 오히려 성능이 떨어진다.
- **핵심 아이디어:** 본 모델의 학습 데이터 분포를 prior로 넣어, canonicalization 결과가 항상 본 모델에게 익숙한 방향이 되도록 규제한다.

이게 이 계열에서 **가장 중요한 함정**입니다. 앞단 모델이 "이게 정방향이야"라고 정한 방향이 반드시 본 모델이 좋아하는 방향이라는 보장이 없습니다. 예를 들어 앞단이 모든 이미지를 45도 기울어진 상태로 통일해버려도 "일관성"은 만족하지만 본 모델은 처음 보는 그림이라 더 헤맵니다. <cite index="30-1">저자들은 canonicalization network가 만든 방향이 학습 분포와 어긋나 성능을 해치는 걸 관찰했고, 데이터셋 의존적 prior로 canonicalization function을 유도해 성능을 유지한 채 회전 강건성을 확보했습니다.</cite> <cite index="31-1">구체적으로는 예측된 방향 분포와 사전학습 데이터의 방향 분포 사이 KL을 최소화하는 prior 규제를 씁니다.</cite> <cite index="39-1">이 아이디어는 SAM 같은 대형 파운데이션 모델까지 확장되며, 파라미터와 추론 속도 증가는 미미했습니다.</cite>

**→ 아이디어 씨앗:** "본 모델이 늘 in-distribution 샘플만 보게 한다"가 설계 원칙. 그런데 **VLM에는 이 prior를 어떻게 정의할지가 미해결**입니다. CNN 분류기는 학습 데이터의 방향 분포를 세면 되지만, VLM의 "익숙한 방향"은 뭘로 측정할까요? 본 모델의 **토큰 확률(perplexity)이나 답변 confidence를 앞단 모델의 보상 신호로 쓰는 것**이 자연스러운 후보이고, 제가 본 범위에선 아직 안 나온 조합입니다.

## A-3. RECON (arXiv 2505.13289)

- **기존 문제점:** 위 두 방식은 canonicalization module이 특정 본 모델에 묶여서 함께 학습돼야 한다.
- **핵심 아이디어:** canonicalizer를 본 모델과 **무관하게 독립적으로** 학습시켜, 같은 입력 도메인을 쓰는 어떤 모델 앞에나 꽂을 수 있게 한다.

<cite index="34-1">STN은 작은 네트워크로 입력을 변형하지만 예측기와 함께 학습해야 하고, EquiAdapt는 얼린 예측기 앞에 canonicalizer를 두되 그 학습이 특정 사전학습 모델과 크기에 묶입니다. 반면 이 방식의 canonicalization은 downstream 모델과 독립적으로 학습되어 같은 입력 도메인의 어떤 모델 앞에도 붙일 수 있습니다.</cite>

**→ 아이디어 씨앗:** A-2와 정확히 상충하는 주장입니다(본 모델에 맞출 것인가 vs 독립시킬 것인가). 사용자 구상은 소형 모델을 한 번 만들어 여러 본 모델에 재사용하는 그림에 가까우니 A-3 편인데, 그러면 A-2가 지적한 misalignment를 어떻게 피할지가 숙제입니다. **"본 모델별로 얇은 어댑터만 갈아끼우는 절충안"**이 빈 자리로 보입니다.

## A-4. Artificial Mental Rotation (arXiv 2311.08525)

- **기존 문제점:** 회전 강건성을 얻는 표준 방법인 data augmentation은 회전 종류가 늘수록 비효율적이다.
- **핵심 아이디어:** 사람의 "멘탈 로테이션"을 흉내내, 입력을 먼저 바로 세우는(correction) 단계를 넣고 그 다음에 분류한다.

<cite index="28-1">ImageNet 샘플을 실제로 인쇄해 12개 각도에서 촬영한 뒤, 원본 상태(raw)와 멘탈 로테이션 보정 후(corrected)를 비교합니다.</cite> 동시에 이 논문은 **자기 자신에 대한 경고**도 담고 있습니다. <cite index="28-1">인위적 데이터 변형에 기반한 self-supervised 학습은 늘 조심해야 하는데, 모델이 원하는 과제를 푸는 건지 self-supervision 절차의 의도치 않은 지름길을 찾은 건지 불분명할 때가 많고, 디지털 회전 알고리즘은 사람 눈엔 안 보여도 알고리즘 특유의 아티팩트를 남기기 때문입니다.</cite>

**→ 아이디어 씨앗:** 앞단 모델을 "디지털로 회전시킨 데이터"로 학습시키면 **리샘플링 아티팩트를 shortcut으로 배울 위험**이 있습니다. 실험 설계에서 통제해야 할 요소이자, 반대로 **"실제 촬영 회전 vs 디지털 회전"에서 성능 차이를 보여주는 것 자체가 논문 한 편의 관찰**이 될 수 있습니다.

## A-5. RotNet 계열 (Gidaris et al., ICLR 2018 + 후속 보정 파이프라인)

- **기존 문제점:** 분류기를 회전에 강건하게 만들려면 학습 때 회전 augmentation을 넣는 게 전부였다.
- **핵심 아이디어:** 이미지에 걸린 90° 단위 회전을 맞히는 네트워크를 따로 두고, 그 예측대로 되돌린 뒤 분류기에 넣는다.

가장 단순하고 오래된 형태의 "앞단 모델"입니다. <cite index="26-1">회전을 맞히려면 네트워크가 이미지 안의 주요 물체를 찾아내고, 그 방향을 인식하고, 물체 종류별로 원래 어떤 방향이 자연스러운지까지 연결지어야 합니다.</cite> 즉 **회전 맞히기 자체가 이미 상당한 semantic 이해를 요구**합니다. 실제 파이프라인 효과도 보고돼 있습니다. <cite index="20-1">CIFAR10에서 94% 정확도의 분류기가 회전된 입력에서는 59.1%로 떨어졌는데, RotNet으로 회전을 보정해 넣자 75.8%까지 회복됐습니다.</cite> 다만 한계도 분명합니다. <cite index="29-1">회전은 실제로 모든 이미지에 대해 결정 가능한 게 아니어서, 방향이 애매한 이미지들에서는 이 방식이 잘 동작하지 않습니다.</cite>

**→ 아이디어 씨앗:** 94 → 59.1 → 75.8 이라는 숫자가 핵심입니다. **보정을 해도 원본 성능(94%)에 한참 못 미칩니다.** 앞단 파이프라인의 근본 한계를 보여주는 수치이고, 사용자 과제가 넘어야 할 벽이 여기 있습니다. "왜 완전 회복이 안 되는가"(보정 오류 전파 + 리샘플링 손실)를 분해하는 것만으로도 분석 논문이 됩니다.

---

# B. 본 모델이 스스로 툴콜해서 회전시키기 ★

## B-0. Thinking with Images for Multimodal Reasoning: Foundations, Methods, and Future Frontiers (arXiv 2506.23918) — **B그룹 전체의 지도**

> **먼저 읽어야 할 논문.** B그룹(특히 B-1 CodeVision)의 아이디어는 이 서베이가 정리한 지형 위에 놓고 봐야 이해됩니다. CodeVision이 "우리는 code-as-tool이다"라고 말할 때, 그게 어느 칸을 차지하는 주장인지 알려주는 게 이 논문입니다.

- **기존 문제점:** 기존 multimodal reasoning은 textual Chain-of-Thought에 기대는데, 이 text-centric 방식은 **vision을 정적인 초기 context로만 취급**한다. 그래서 풍부한 perceptual data와 이산적인 symbolic thought 사이에 **semantic gap**이 생긴다.
- **핵심 아이디어:** vision을 passive input이 아니라 **동적이고 조작 가능한 cognitive workspace**로 승격시킨다. 그리고 이 계열 전체를 **cognitive autonomy가 커지는 3단계**로 정리한다.

사람은 어려운 문제를 풀 때 머릿속으로만 생각하지 않습니다. 종이에 그림을 그리고, 화살표를 치고, 중요한 데 동그라미를 칩니다. 기존 MLLM은 이걸 못 했어요 — 이미지를 한 번 보고 말로 옮긴 다음에는, 그 말만 가지고 끝까지 추론했습니다. 처음에 놓친 건 영영 못 봅니다. 이 서베이가 정리하는 새 흐름은 **모델에게 "다시 보기"와 "그려보기"를 허용**하는 것입니다.

<cite index="17-1">"Thinking about Images"는 정적인 그림에 대한 관찰을 말로 옮긴 뒤 답을 내지만 추론 중 시각 콘텐츠 자체는 바뀌지 않습니다. 반면 "Thinking with Images"는 그림을 능동적 외부 작업공간으로 승격시켜, 모델이 생성·크롭·강조·스케치·주석을 추론 체인의 토큰처럼 반복 삽입합니다.</cite>

**3단계 분류 (cognitive autonomy 증가 순)**

| Stage | 이름 | 모델의 역할 | 무엇을 하나 |
|---|---|---|---|
| **1** | Tool-Driven Visual Exploration | **Commander** | 외부 tool을 골라서 지시. 지능은 "어떤 tool을 쓸까"에서 나옴 |
| **2** | Programmatic Visual Manipulation | **Visual Programmer** | 코드를 생성해 임의의 시각 연산을 조합 |
| **3** | Intrinsic Visual Imagination | **Visual Thinker** | 외부 도구 없이 **머릿속에서 이미지를 생성**해 추론 단계로 삼음 |

**→ 아이디어 씨앗 (3개):**

1. **B-1 CodeVision은 Stage 2에 정확히 들어갑니다.** "code-as-tool"이 곧 Programmatic Visual Manipulation이고, RL로 학습시켰으니 Stage 2 × RL 칸입니다. **즉 CodeVision의 novelty는 "새 단계를 열었다"가 아니라 "이미 있던 Stage 2를 회전 문제에 잘 적용했다"입니다.** 이걸 알면 CodeVision을 과대평가하지 않게 됩니다.

2. **A그룹(canonicalization)은 이 taxonomy 어디에도 없습니다.** 서베이가 다루는 건 전부 "본 모델이 스스로 하는" 계열이거든요. 회전이라는 같은 문제를 A그룹은 완전히 다른 언어(equivariance)로 다뤄왔는데, 두 계열이 서로를 모릅니다. **이 단절 자체가 우리 논문의 자리입니다.**

3. **가장 중요한 것 — 이 taxonomy의 축은 "본 모델의 cognitive autonomy"입니다.** Stage 1→3은 전부 *본 모델이 얼마나 스스로 하느냐*로 매겨집니다. 그런데 **사용자 구상(본 모델 freeze + 별도 앞단 모델)은 이 축에 아예 안 올라갑니다.** 앞단 모델이 자율적으로 tool을 부르지만 그건 본 모델의 autonomy가 아니니까요. 서베이의 축이 못 담는 설계라는 것 — **이게 novelty를 주장하는 가장 깔끔한 프레임**이 될 수 있습니다.

*(같은 용도의 보조 자료: Agentic MLLM 서베이 arXiv 2510.10991.)*

## B-1. CodeVision (CVPR 2026, arXiv 2512.03746, ZJU + ByteDance) — **이 과제에 가장 직접적**

- **기존 문제점:** thinking-with-images 계열은 crop/zoom 같은 좁은 툴 집합만 쓰고, 정작 최신 MLLM은 **단순한 방향 변화에도 크게 무너진다는 게 그동안 간과돼 왔다.**
- **핵심 아이디어:** 툴 목록을 고정하지 말고, 모델이 **코드를 생성해 임의의 이미지 연산을 호출**하게 한다(code-as-tool).

<cite index="55-1">저자들은 먼저 최신 MLLM조차 단순한 방향 변화나 자연스러운 손상(corruption)에 성능이 크게 떨어진다는, 그동안 간과됐던 취약점을 드러냅니다. 이를 해결하기 위해 고정된 툴 레지스트리를 벗어나 코드를 범용 인터페이스로 삼는 CodeVision을 제안하고, 복잡한 multi-turn 툴 조합과 에러 복구를 담은 SFT 데이터로 1단계 학습을 한 뒤, 전략적이고 효율적인 툴 사용을 유도하는 dense process reward로 RL을 돌립니다. 또한 방향 변화 강건성과 multi-tool reasoning을 엄밀히 평가하는 새 벤치마크 스위트를 만들었고, Qwen2.5-VL·Qwen3-VL 계열 실험에서 유연한 툴 조합, 효율적 체이닝, 런타임 피드백 기반 에러 복구 같은 emergent 능력이 나타났습니다.</cite>

데이터 구축 방식도 참고할 만합니다. <cite index="19-1">메타데이터를 샘플링한 뒤 그에 맞춰 원본(정방향) 이미지를 변형해 모델의 초기 관찰을 만드는데, 예를 들어 필요한 툴이 rotate-180이면 원본을 180도 돌려서 입력으로 줍니다. 또 일부러 잘못된 툴 사용이나 런타임 에러를 넣어, 모델이 에러 로그를 읽고 코드를 고쳐 재시도하다가 결국 올바른 툴로 갈아타게 하는 error-handling 데이터도 만듭니다.</cite>

**→ 이 논문이 사실상 과제의 경쟁 논문입니다.** 가장 먼저 정독하고 "이 대비 뭐가 다른가"를 명확히 잡아야 novelty가 섭니다. 현재 눈에 띄는 차별점: CodeVision은 **본 모델 하나를 통째로 학습**시키는데, 사용자 구상은 **본 모델을 얼리고 앞단만 학습**합니다. 후자는 본 모델을 갈아끼울 수 있고 학습 비용이 훨씬 싸다는 실용적 장점이 있습니다. 코드 공개(ByteDance-BandAI/CodeVision).

## B-2. Thyme (arXiv 2508.11630)

- **기존 문제점:** 미리 정해둔 crop/zoom 툴만으로는 실제로 필요한 이미지 조작을 다 커버할 수 없다.
- **핵심 아이디어:** 추론 과정 안에서 코드를 생성해 자율적으로 이미지를 편집하고, **언제 어떻게 조작할지 스스로 결정**하게 한다.

<cite index="15-1">이 접근은 크롭·회전·대비 강화 같은 즉석 이미지 조작뿐 아니라 수학 계산까지 가능하게 하며, 언제 어떤 연산을 쓸지에 대한 높은 자율성을 유지합니다. 500K 규모 큐레이션 데이터로 코드 생성을 가르치는 SFT를 먼저 하고, 그다음 의사결정을 다듬는 RL을 붙이는 2단계 전략을 씁니다. RL 단계에서는 GRPO-ATS(Adaptive Temperature Sampling)를 제안해 텍스트와 코드 생성에 서로 다른 온도를 적용, 추론 탐색과 코드 실행 정확도를 균형 맞춥니다.</cite>

**→ 아이디어 씨앗:** "텍스트와 코드에 다른 샘플링 온도"라는 트릭은 툴콜 RL 일반에 적용 가능합니다. 진행 중이신 MOPD 쪽에도 그대로 옮겨볼 만한 아이디어입니다(추론 토큰은 탐색이 필요하고 툴콜 인자는 정확해야 한다 — function calling에서도 똑같이 성립).

## B-3. DeepEyes (ICLR 2026, arXiv 2505.14362)

- **기존 문제점:** thinking-with-images를 하려면 보통 cold-start SFT 데이터를 미리 모으거나 외부 전문 모델을 붙여야 한다.
- **핵심 아이디어:** SFT 없이 end-to-end RL만으로, **모델 자신의 grounding 능력을 툴처럼 쓰는 행동을 emerge**시킨다.

<cite index="60-1">DeepEyes는 cold-start SFT용 사전 수집 데이터 없이 end-to-end RL로 "이미지로 사고하기"를 학습합니다. 주목할 점은 이 능력이 외부 전문 모델이나 API에 의존하지 않고 모델 자체의 grounding 능력을 내재 함수로 활용해 자연히 emerge한다는 것입니다.</cite> <cite index="61-1">7B 모델만으로도 여러 벤치마크에서 경쟁력 있는 결과를 냈고, visual search나 비교 같은 다양한 추론 행동을 보입니다.</cite>

**→ 사용자 아이디어에 대한 가장 강력한 반론 논문입니다.** "앞단에 별도 소형 모델을 붙일 필요 없이 본 모델이 스스로 하게 하면 된다"는 주장이므로, 앞단 모델 방식을 밀려면 **왜 회전에서는 이게 안 통하는지**를 논증해야 합니다. 반박 논거 후보: DeepEyes가 emerge시킨 건 grounding(어디를 볼까)인데 **회전 판정은 grounding이 아니라 canonical prior(원래 어느 방향이어야 하나)를 요구**하고, 이건 본 모델 안에 애초에 없거나 약한 지식이라 RL로 emerge시킬 재료가 없다 — 90°/270° 실패가 그 증거.

## B-4. AdaTooler-V (ACL Findings 2026, arXiv 2512.16918)

- **기존 문제점:** 오픈소스 모델들이 툴이 필요 없을 때도 무작정 불러대서 추론 비용이 늘고 성능도 오히려 떨어진다.
- **핵심 아이디어:** 샘플별로 "이 문제에 툴이 실제로 도움이 되나"를 점수화해, 도움될 때만 툴을 부르도록 보상을 조절한다.

<cite name="" index="68-1">기존 오픈소스 모델은 시각 툴이 불필요한 경우에도 호출하는 맹목적 툴 사용 패턴을 보여 추론 오버헤드가 커지고 성능이 저하됩니다. AdaTooler-V는 시각 문제가 정말 툴을 필요로 하는지 판단하는 적응적 툴 사용을 수행하며, 샘플별 Tool Benefit Score에 따라 보상 스케일을 조정하는 AT-GRPO를 도입해 실제 개선이 있을 때만 툴을 부르도록 유도합니다.</cite> <cite index="75-1">여기서 점수는 Qwen2.5-VL-72B가 같은 샘플을 툴 있이/없이 풀었을 때의 평균 정확도 차이로 계산합니다.</cite>

**→ 아이디어 씨앗:** 회전 과제에 그대로 옮기면 **"돌릴까 말까"를 판단하는 문제**가 됩니다. 방향이 애매한 이미지(하늘, 텍스처)에는 아예 손대지 않는 게 낫고, 잘못 돌리면 오히려 손해입니다. **Tool Benefit Score를 "회전 보정 이득"으로 재정의**해 앞단 모델의 abstain(그냥 통과) 정책을 학습시키는 건 A 그룹과 B 그룹을 잇는 자연스러운 다리입니다.

## B-5. OpenThinkIMG (arXiv 2505.08617) / ReVPT / Visual Sketchpad — 툴셋 계열

- **기존 문제점:** 모델이 쓸 수 있는 시각 도구가 너무 빈약하다.
- **핵심 아이디어:** 크롭·줌·그리기 같은 기본 연산에 SAM, GroundingDINO, 깊이추정 같은 외부 전문 모델까지 툴로 묶어준다.

<cite index="10-1">OpenThinkIMG는 복잡한 차트 추론을 위해 적응적 툴 사용이 가능한 agentic MLLM을 만들며, 툴셋은 crop·zoomin·draw 같은 기본 연산과 SAM·GroundingDINO 같은 강력한 외부 모델을 함께 포함합니다. VILASR은 여기서 나아가 바운딩 박스나 보조선을 그려 넣게 해 공간 지능을 높이고, ReVPT는 깊이 추정·줌인·객체 탐지·엣지 검출을 아우르는 종합 시각 툴킷을 도입해 cold-start SFT와 RL로 공간 추론과 이미지 이해에서 SOTA를 세웠습니다.</cite> <cite index="18-1">Visual Sketchpad는 추론 중에 그리고·자르고·주석할 수 있는 편집 가능한 캔버스를 제공했고, OpenAI o3가 추론 체인 안에 zoom·crop·rotate를 제품화하면서 오픈 진영의 재현 연구가 촉발됐습니다.</cite>

**→ 아이디어 씨앗:** **o3는 이미 rotate를 툴로 갖고 있는데도 회전 판정에 실패합니다.** 즉 "툴이 있느냐"가 아니라 "언제 얼마나 돌릴지 판단할 수 있느냐"가 병목이라는 뜻이고, 이건 사용자 과제의 문제 정의를 날카롭게 해주는 관찰입니다.

---

# C. 툴 없이 그 능력을 모델 안에 넣기 (distillation)

## C-1. Zooming without Zooming (Wei et al., 2026)

- **기존 문제점:** 추론할 때마다 툴을 부르면 지연·비용이 붙고, 미리 정의된 연산 집합에 묶인다.
- **핵심 아이디어:** 툴을 쓰는 이득을 **학습 단계에서 모델 안으로 distill**해, 추론 시엔 툴 없이도 같은 효과를 내게 한다.

<cite index="11-1">이 연구는 추론 시점의 명시적 툴 사용이 정말 필요한지를 의심하고, 영역 단위 관찰의 이득을 학습 중에 모델로 distill하는 방식을 제안합니다.</cite> 같은 문제의식이 여러 곳에서 반복됩니다. <cite index="13-1">기존 방법들은 명시적 툴 호출, multi-turn 상호작용, 추가 시각 처리를 요구해 지연이 늘고 추론이 사전 정의된 연산 집합에 의존하게 됩니다.</cite>

**→ 아이디어 씨앗:** MOPD(소형 모델 tool-calling distillation) 연구와 직접 이어집니다. 다만 **회전은 crop/zoom과 성질이 다릅니다.** crop은 "이미 본 픽셀을 다시 자세히 보는" 것이라 distill이 되지만, 회전은 **본 모델의 vision encoder가 애초에 그 방향을 학습한 적이 없으면** 내재화할 표현 자체가 없을 수 있습니다. "**어떤 시각 툴이 distill 가능하고 어떤 건 불가능한가**"를 회전 vs 줌으로 대조 실험하는 건 꽤 깔끔한 논문 프레임입니다.

## C-2. Visual Program Distillation (CVPR 2024, Hu et al.)

- **기존 문제점:** 툴·프로그램 기반 시각 추론은 강력하지만 파이프라인이 무겁고 느리다.
- **핵심 아이디어:** 프로그램 실행으로 얻은 정답 추론 과정을 VLM 하나에 distill해 넣는다.

C-1의 원조 격입니다. 툴 파이프라인이 만들어낸 좋은 궤적을 교사 신호로 삼아, 학생 VLM이 툴 없이 한 번에 답하게 만듭니다.

**→ "앞단 모델 vs distillation" 비교 실험을 설계한다면 distillation 쪽 베이스라인.**

---

# 아이디어 종합 — 빈 자리로 보이는 것 5개

위 논문들을 겹쳐 봤을 때 아직 아무도 안 채운 것으로 보이는 지점들입니다. (제 판단이므로 관련 논문 유무는 추가 확인이 필요합니다.)

1. **canonicalizer를 "미분 가능한 회귀 모듈"에서 "툴콜하는 소형 reasoning 모델"로 교체** — A-1의 이론틀 + B 계열의 툴콜을 합치는 것. 정당화 근거는 A-5(회전 판정이 semantic 이해를 요구함)와 90°/270° 실패.
2. **본 모델의 확률/confidence를 앞단 모델의 보상으로** — A-2의 prior 규제를 VLM으로 옮기는 문제의 답. 본 모델을 얼린 채 앞단만 RL로 돌리는, 값싼 학습 루프가 됩니다.
3. **회전 abstain 정책** — B-4의 Tool Benefit Score를 회전에 재정의. "방향이 정의되지 않는 이미지는 건드리지 않는다"를 학습시키는 것. A-5가 지적한 한계에 대한 직접적 답이기도 합니다.
4. **distillation 가능성의 경계 규명** — C-1을 뒤집어, 줌은 distill되는데 회전은 안 되는지를 대조 실험. 안 된다면 그게 곧 앞단 모델이 필요한 이유가 됩니다.
5. **오차 전파 분해** — A-5의 94 → 59.1 → 75.8에서 잃어버린 18%p가 보정 오류 때문인지 리샘플링 손실 때문인지 분리. 전자면 앞단 모델을 키우면 되고, 후자면 파이프라인 자체가 상한에 걸린 겁니다. **이걸 먼저 확인하지 않으면 방향 전체가 헛돌 수 있습니다.**

# 읽는 순서 제안

0. **Thinking with Images 서베이 (B-0)** — **지도 먼저.** 아래 논문들이 각각 어느 칸에 있는지 알고 읽어야 함
1. **CodeVision (B-1)** — 경쟁 논문. 여기서 뭐가 남았는지 봐야 방향이 잡힘
2. **EquiAdapt (A-2)** — 앞단 모델 방식의 핵심 함정과 해법
3. **DeepEyes (B-3)** — 앞단 모델 없이도 된다는 반론. 이걸 못 넘으면 novelty가 약해짐
4. **AdaTooler-V (B-4)** — "언제 툴을 부를까"의 최신 답
5. 나머지는 필요할 때
